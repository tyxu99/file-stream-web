import svgPanZoom from "svg-pan-zoom";
import { useEffect } from "react";

const Index = () => {
  // const panZoomContainer = svgPanZoom("#svgPanZoom");

  // useEffect(() => {
  //   if (document && window) {
  //     const el = document.getElementById("svgPanZoom");
  //     if (el) {
  //       const inst = svgPanZoom(el);
  //     }
  //   }
  // }, []);

  return <div id="svgPanZoom"></div>;
};

export default Index;
